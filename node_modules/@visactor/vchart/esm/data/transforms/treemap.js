import { TreemapLayout } from "@visactor/vgrammar-hierarchy";

export const treemap = (data, op) => {
    if (!data || !(null == op ? void 0 : op.range)) return data;
    const range = op.range();
    if (range.x1 - range.x0 == 0 || range.y1 - range.y0 == 0) return data;
    return new TreemapLayout(op).layout(data, range);
};
//# sourceMappingURL=treemap.js.map

import { registerGaugePointerSeries, registerGaugeSeries } from "../../series/gauge";

import { SeriesTypeEnum } from "../../series/interface/type";

import { ChartTypeEnum } from "../interface/type";

import { Factory } from "../../core/factory";

import { GaugeChartSpecTransformer } from "./gauge-transformer";

import { BaseChart } from "../base";

export class GaugeChart extends BaseChart {
    constructor() {
        super(...arguments), this.transformerConstructor = GaugeChartSpecTransformer, this.type = ChartTypeEnum.gauge, 
        this.seriesType = SeriesTypeEnum.gaugePointer;
    }
}

GaugeChart.type = ChartTypeEnum.gauge, GaugeChart.seriesType = SeriesTypeEnum.gaugePointer, 
GaugeChart.view = "singleDefault", GaugeChart.transformerConstructor = GaugeChartSpecTransformer;

export const registerGaugeChart = () => {
    registerGaugePointerSeries(), registerGaugeSeries(), Factory.registerChart(GaugeChart.type, GaugeChart);
};
//# sourceMappingURL=gauge.js.map

import { SeriesTypeEnum } from "../../series/interface/type";

import { ChartTypeEnum } from "../interface/type";

import { registerLineSeries } from "../../series/line/line";

import { Factory } from "../../core/factory";

import { LineChartSpecTransformer } from "./line-transformer";

import { BaseChart } from "../base";

export class LineChart extends BaseChart {
    constructor() {
        super(...arguments), this.transformerConstructor = LineChartSpecTransformer, this.type = ChartTypeEnum.line, 
        this.seriesType = SeriesTypeEnum.line, this._canStack = !0;
    }
}

LineChart.type = ChartTypeEnum.line, LineChart.seriesType = SeriesTypeEnum.line, 
LineChart.view = "singleDefault", LineChart.transformerConstructor = LineChartSpecTransformer;

export const registerLineChart = () => {
    registerLineSeries(), Factory.registerChart(LineChart.type, LineChart);
};
//# sourceMappingURL=line.js.map

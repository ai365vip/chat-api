import { BaseChart } from "../base/base-chart";

import { ChartTypeEnum } from "../interface/type";

import { Factory } from "../../core/factory";

import { CommonChartSpecTransformer } from "./common-transformer";

export class CommonChart extends BaseChart {
    constructor() {
        super(...arguments), this.transformerConstructor = CommonChartSpecTransformer, this.type = ChartTypeEnum.common, 
        this._canStack = !0;
    }
}

CommonChart.type = ChartTypeEnum.common, CommonChart.view = "singleDefault", CommonChart.transformerConstructor = CommonChartSpecTransformer;

export const registerCommonChart = () => {
    Factory.registerChart(CommonChart.type, CommonChart);
};
//# sourceMappingURL=common.js.map

import { registerRoseSeries } from "../../series/rose/rose";

import { SeriesTypeEnum } from "../../series/interface/type";

import { ChartTypeEnum } from "../interface/type";

import { Factory } from "../../core/factory";

import { RoseChartSpecTransformer } from "./rose-transformer";

import { BaseChart } from "../base";

export class RoseChart extends BaseChart {
    constructor() {
        super(...arguments), this.transformerConstructor = RoseChartSpecTransformer, this.type = ChartTypeEnum.rose, 
        this.seriesType = SeriesTypeEnum.rose, this._canStack = !0;
    }
}

RoseChart.type = ChartTypeEnum.rose, RoseChart.seriesType = SeriesTypeEnum.rose, 
RoseChart.view = "singleDefault", RoseChart.transformerConstructor = RoseChartSpecTransformer;

export const registerRoseChart = () => {
    registerRoseSeries(), Factory.registerChart(RoseChart.type, RoseChart);
};
//# sourceMappingURL=rose.js.map

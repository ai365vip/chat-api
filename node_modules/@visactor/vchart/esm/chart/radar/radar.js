import { registerRadarSeries } from "../../series/radar/radar";

import { SeriesTypeEnum } from "../../series/interface/type";

import { ChartTypeEnum } from "../interface/type";

import { Factory } from "../../core/factory";

import { RadarChartSpecTransformer } from "./radar-transformer";

import { BaseChart } from "../base";

export class RadarChart extends BaseChart {
    constructor() {
        super(...arguments), this.transformerConstructor = RadarChartSpecTransformer, this.type = ChartTypeEnum.radar, 
        this.seriesType = SeriesTypeEnum.radar, this._canStack = !0;
    }
}

RadarChart.type = ChartTypeEnum.radar, RadarChart.seriesType = SeriesTypeEnum.radar, 
RadarChart.view = "singleDefault", RadarChart.transformerConstructor = RadarChartSpecTransformer;

export const registerRadarChart = () => {
    registerRadarSeries(), Factory.registerChart(RadarChart.type, RadarChart);
};
//# sourceMappingURL=radar.js.map

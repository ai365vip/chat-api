import { isPlainObject } from "@visactor/vutils";

import { getActualColor, isColorKey } from "../../theme/color-scheme/util";

const IGNORE_KEYS = [ "animationThreshold", "colorScheme", "fontFamily", "name", "padding" ];

export function preprocessTheme(obj, colorScheme, seriesSpec) {
    if (!obj) return obj;
    const newObj = {};
    return Object.keys(obj).forEach((key => {
        const value = obj[key];
        IGNORE_KEYS.includes(key) ? newObj[key] = value : isPlainObject(value) ? isColorKey(value) ? newObj[key] = getActualColor(value, colorScheme, seriesSpec) : newObj[key] = preprocessTheme(value, colorScheme, seriesSpec) : newObj[key] = value;
    })), newObj;
}
//# sourceMappingURL=preprocess.js.map

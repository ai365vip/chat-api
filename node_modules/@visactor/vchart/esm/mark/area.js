import { Factory } from "./../core/factory";

import { BaseLineMark } from "./base/base-line";

import { MarkTypeEnum } from "./interface/type";

import { registerAreaGraphic } from "@visactor/vgrammar-core";

import { registerVGrammarLineOrAreaAnimation } from "../animation/config";

export class AreaMark extends BaseLineMark {
    constructor() {
        super(...arguments), this.type = AreaMark.type;
    }
    _getDefaultStyle() {
        return Object.assign(Object.assign({}, super._getDefaultStyle()), {
            lineWidth: 0
        });
    }
    _getIgnoreAttributes() {
        return [];
    }
}

AreaMark.type = MarkTypeEnum.area;

export const registerAreaMark = () => {
    Factory.registerMark(AreaMark.type, AreaMark), registerAreaGraphic(), registerVGrammarLineOrAreaAnimation();
};
//# sourceMappingURL=area.js.map

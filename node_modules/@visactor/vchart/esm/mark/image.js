import { Factory } from "./../core/factory";

import { BaseMark } from "./base/base-mark";

import { MarkTypeEnum } from "./interface/type";

import { registerImageGraphic } from "@visactor/vgrammar-core";

export class ImageMark extends BaseMark {
    constructor() {
        super(...arguments), this.type = ImageMark.type;
    }
    _getDefaultStyle() {
        return Object.assign(Object.assign({}, super._getDefaultStyle()), {
            width: void 0,
            height: void 0,
            lineWidth: 0
        });
    }
}

ImageMark.type = MarkTypeEnum.image;

export const registerImageMark = () => {
    Factory.registerMark(ImageMark.type, ImageMark), registerImageGraphic();
};
//# sourceMappingURL=image.js.map

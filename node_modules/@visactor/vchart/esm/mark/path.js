import { Factory } from "./../core/factory";

import { BaseMark } from "./base/base-mark";

import { MarkTypeEnum } from "./interface/type";

import { registerPathGraphic } from "@visactor/vgrammar-core";

export class PathMark extends BaseMark {
    constructor() {
        super(...arguments), this.type = PathMark.type;
    }
    _getDefaultStyle() {
        return Object.assign(Object.assign({}, super._getDefaultStyle()), {
            lineWidth: 0,
            path: ""
        });
    }
}

PathMark.type = MarkTypeEnum.path;

export const registerPathMark = () => {
    Factory.registerMark(PathMark.type, PathMark), registerPathGraphic();
};
//# sourceMappingURL=path.js.map

export function readCSVTopNLine(csvFile, n) {
    let res = "";
    return [ "\r\n", "\r", "\n" ].some((splitter => !!csvFile.includes(splitter) && (res = csvFile.split(splitter).slice(0, n + 1).join(splitter), 
    !0))) ? res : csvFile;
}
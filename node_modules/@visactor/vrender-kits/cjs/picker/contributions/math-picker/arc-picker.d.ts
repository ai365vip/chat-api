import type { IPoint } from '@visactor/vutils';
import type { IArc, IGraphicPicker, IGraphicRender, IPickParams } from '@visactor/vrender-core';
export declare class DefaultMathArcPicker implements IGraphicPicker {
    readonly canvasRenderer: IGraphicRender;
    type: string;
    numberType: number;
    constructor(canvasRenderer: IGraphicRender);
    contains(arc: IArc, point: IPoint, params?: IPickParams): boolean;
}

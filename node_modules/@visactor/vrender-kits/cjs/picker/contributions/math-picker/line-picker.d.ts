import type { IPoint } from '@visactor/vutils';
import type { ILine, IGraphicPicker, IGraphicRender, IPickParams } from '@visactor/vrender-core';
export declare class DefaultMathLinePicker implements IGraphicPicker {
    readonly canvasRenderer: IGraphicRender;
    type: string;
    numberType: number;
    constructor(canvasRenderer: IGraphicRender);
    contains(line: ILine, point: IPoint, params?: IPickParams): boolean;
}

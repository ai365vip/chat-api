import type { IPoint } from '@visactor/vutils';
import type { IRect, IGraphicPicker, IGraphicRender, IPickParams } from '@visactor/vrender-core';
export declare class DefaultMathRectPicker implements IGraphicPicker {
    readonly canvasRenderer: IGraphicRender;
    type: string;
    numberType: number;
    constructor(canvasRenderer: IGraphicRender);
    contains(rect: IRect, point: IPoint, params?: IPickParams): boolean;
}

import type { IPoint } from '@visactor/vutils';
import type { IArea, IGraphicPicker, IGraphicRender, IPickParams } from '@visactor/vrender-core';
export declare class DefaultMathAreaPicker implements IGraphicPicker {
    readonly canvasRenderer: IGraphicRender;
    type: string;
    numberType: number;
    constructor(canvasRenderer: IGraphicRender);
    contains(area: IArea, point: IPoint, params?: IPickParams): boolean;
}

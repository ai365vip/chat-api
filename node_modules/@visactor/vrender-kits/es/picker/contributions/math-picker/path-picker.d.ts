import type { IPoint } from '@visactor/vutils';
import type { IPath, IGraphicPicker, IGraphicRender, IPickParams } from '@visactor/vrender-core';
export declare class DefaultMathPathPicker implements IGraphicPicker {
    readonly canvasRenderer: IGraphicRender;
    type: string;
    numberType: number;
    constructor(canvasRenderer: IGraphicRender);
    contains(path: IPath, point: IPoint, params?: IPickParams): boolean;
}

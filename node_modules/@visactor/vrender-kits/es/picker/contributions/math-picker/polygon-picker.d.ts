import type { IPoint } from '@visactor/vutils';
import type { IPolygon, IGraphicPicker, IGraphicRender, IPickParams } from '@visactor/vrender-core';
export declare class DefaultMathPolygonPicker implements IGraphicPicker {
    readonly canvasRenderer: IGraphicRender;
    type: string;
    numberType: number;
    constructor(canvasRenderer: IGraphicRender);
    contains(polygon: IPolygon, point: IPoint, params?: IPickParams): boolean;
}
